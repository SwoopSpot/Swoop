import React from 'react';
import NavBar from './NavBar';

function Account() {

/*
[] Should have a payments section (review payments, payouts, manage payment methods)
[] Should have a personal information section (name, email address, phone number, id, address)
[] should have a login section (update password, deactivate account)
[] should have a notifications section (how you want to be contacted, preferences)

this is separate from the public profile page where you have an about me, your rating etc)
*/

return (
    <div>
        <NavBar />
    </div>
)

}

export default Account;